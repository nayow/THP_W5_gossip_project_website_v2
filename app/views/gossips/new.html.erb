<h1 class="display-4 text-center">Balance ton potin!</h1>
<hr><br/>
<div class="container" style="align-items:center;">
  <div class="text-center" style="margin-left:30%; margin-right:30%;">
    <%= form_tag url_for(action: 'create'), method: "post" do %>
      <%= text_field_tag 'title' ,"", class:"form-control", placeholder:"Titre" %><br/>
      <%= text_area_tag 'content' ,"", class:"form-control", placeholder:"Contenu" %><br/>
      <%= select_tag 'Tag', options_for_select(Tag.all.pluck('title')), class:"form-control" %><br/>
      <%= submit_tag 'Envoyer', class:"btn btn-secondary" %>
    <% end %>
  </div>
  <br/>
  <% if @gossip.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
      <ul>
        <% @gossip.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
</div>